<div>
    <h2>
    {% if page.h2.uppercase %}
    {{ site.data.resume.header_titles[page.h2.theme_index].skills | upcase }}
    {% else %}
    {{ site.data.resume.header_titles[page.h2.theme_index].skills }}
    {% endif %}
    </h2>
    <div class="thickside">
        <table>
            <tbody>
                {% for verb in site.data.resume.skills %}
                <td class="thinline">
                    <h3>{{ verb.verb | capitalize }}</h3>
                    <p>{{ verb.skills | join: ', ' }}</p>
                </td>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>