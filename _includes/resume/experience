<div>
    <h2>
    {% if page.h2.uppercase %}
    {{ site.data.resume.header_titles[page.h2.theme_index].exp | upcase }}
    {% else %}
    {{ site.data.resume.header_titles[page.h2.theme_index].exp }}
    {% endif %}
    </h2>
    <div class="thickside">
        {% assign i = 0 %}
        {% for exp in site.data.resume.experience %}
        <div class="thinline">
            <h3>{{ exp.position }}</h3>
            <p class="text-muted">
                <span style="font-weight: bold; font-size: 1.2em">{{ exp.company }}</span>&nbsp;&vert;&nbsp;
                <span>{{ exp.start }} - {{ exp.end }}</span>&nbsp;&vert;&nbsp;
                <span>{{ exp.duration }}</span>
            </p>
            <ul class="my-1">
                {% for do in exp.doings %}
                    <li>{{ do }}.</li>
                {% endfor %}
            </ul>
        </div>
        {% assign i = i | plus: 1 %}
            {% if i == include.max_display %}
        {% break %}
        {% endif %}
        {% endfor %}
    </div>
</div>